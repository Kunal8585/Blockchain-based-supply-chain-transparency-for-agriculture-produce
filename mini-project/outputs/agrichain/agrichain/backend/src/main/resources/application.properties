# Application
spring.application.name=agrichain
server.port=8080

# MongoDB
# Note: Ensure MongoDB is running on your Windows machine at this port
spring.data.mongodb.uri=mongodb://localhost:27017/agrichain
spring.data.mongodb.database=agrichain

# Jackson (Formatting dates correctly)
spring.jackson.serialization.write-dates-as-timestamps=false
